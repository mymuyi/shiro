[main]
#声明 realm
myRealm1=chapter2.realm.MyRealm1
myRealm2=chapter2.realm.MyRealm2
#指定securityManager的realms实现，如果不指定 realms，会根据realm声明顺序使用，如果指定了，那么没有指定的 Realm 不会启用。
securityManager.realms=$myRealm1,$myRealm2